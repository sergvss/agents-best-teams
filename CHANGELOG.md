# История изменений

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/), версии — по [семантическому версионированию](https://semver.org/lang/ru/).

---

## [1.4.0] — 2026-08-28

### Добавлено

- **Английский README** — [`README.en.md`](README.en.md), переключатель языка в шапке обоих файлов. GitHub не умеет выбирать язык по локали посетителя, поэтому второй файл и явная ссылка между ними — единственный работающий способ.
- **Раздел «Два языка» в README.** Отвечает на вопрос, который иначе решается догадками: содержимое разложено на три слоя (README — RU и EN, проза методологии — RU, сообщения хуков — RU), для каждого названа причина. Главное сказано прямо: на английском всё работает, потому что роли и скиллы — это промпты, а хуки вообще не текст; упрётся ровно в одном месте — блокировка объясняет причину по-русски.
- **Путь публикации в community-маркетплейс** в памятке автору ([`docs/update.md`](docs/update.md)): подача идёт формой, а не PR, каталог синхронизируется раз в сутки, и проверять факт публикации нужно по имени плагина в `marketplace.json` каталога.
- **Сверка английского README** добавлена в памятку автору как шаг выпуска: перевод отстаёт на выпуск допустимо, расходится по фактам — нет.

### Исправлено

По итогам прогона двух последних eval-сценариев принципа 10 на живом коде. Оба провала оказались не в поведении агента, а в оснастке вокруг него.

- **Проверка шага 3 в `setup-agent-team` давала зелёный свет незаполненной роли.** Она искала плейсхолдеры по угловым скобкам, а раздел «Неприкосновенные инварианты» — прозаическая болванка: «Опиши здесь инварианты своего проекта» и примеры из типового стека. В песочнице роль девять сессий носила требование гонять схему через Alembic в проекте без ORM — это хуже пустого раздела, потому что такое требование выполняют. Добавлен второй grep и прямое указание: инварианты выясняются из кода, неприменимые примеры удаляются, неочевидное спрашивается, а не придумывается.
- **Правило 2-из-3 при отказе провайдера вырождалось молча.** Отчёт со всеми упавшими сторонами выглядел как у нормального прогона: «пересечений между ревьюерами нет» читается как одобрение, хотя пересекаться было нечему. В `principles/05` добавлен подраздел «Когда голосов меньше трёх», в `templates/external-llm-reviewer.md` — раздел «Отказ провайдера» вместо одной строки таблицы: число дошедших голосов рядом с выводом, отказ от вывода о консенсусе при неполном кворуме, ненулевой код возврата.

---

## [1.3.3] — 2026-08-27

### Исправлено

- **README обещал, что всё заработает сразу.** На деле картина сложнее: хуки и скиллы включаются сразу, предложение собрать команду — только со следующей сессии (это хук на `SessionStart`, и при установке посреди работы событие уже прошло), а роли, Stop-хук и триангуляция не включаются сами вообще. Добавлена таблица «что включается когда», а также два условия, без которых не работает ничего: активация плагина и имя интерпретатора Python на macOS и Linux.

---

## [1.3.2] — 2026-08-27

### Исправлено

Вычитка README целиком по следам обратной связи.

- **Сноска «Про название» ссылалась не на тот документ.** Она объясняет разницу между командой агентов и функцией `agent teams`, но стояла в конце раздела «Обновление» и говорила «разбор в документе выше» — то есть указывала на `update.md`. Оторвалась, когда между ней и её документом вставили новый раздел. Возвращена на место, ссылка сделана явной.
- **Обращение скакало между «ты» и «вы».** Весь README написан на «ты», а разделы про обновление и развёртывание команды — на «вы». Читалось как два разных автора.
- **«Полусотней тестов» устарело** — их 84. Точное число убрано: оно уже трижды расходилось с реальностью.
- **Из структуры пропал каталог `.claude-plugin/`** при переписывании README, из-за чего соседнее утверждение «к Claude Code привязаны только hooks и skills» стало неверным. Добавлен и он, и `VERSION`.
- Опечатка «закоммить» и неоднозначное «последняя строка» после вставки абзаца между ней и таблицей.

---

## [1.3.1] — 2026-08-27

### Исправлено

- **Инструкция установки сбивала с толку.** В двух соседних строках слово `sergvss` означает разное: в `marketplace add` это адрес репозитория, в `install` — имя маркетплейса. Догадаться было невозможно, и в окне Manage Plugins люди подставляли в поле Add имя вместо адреса, получая `Invalid marketplace source format`. Различие теперь названо прямо, в README и в `docs/update.md`.

---

## [1.3.0] — 2026-08-27

### Добавлено

Четыре роли по итогам сверки с набором gstack. Ролей стало 16.

- **`security-reviewer`** — аудит безопасности отдельным проходом. Раньше security была одним из пунктов в списке забот `code-reviewer`, то есть ровно тем антипаттерном, против которого написан принцип 01: агент, знающий всё частично, находит поверхностное во всём. Машинерия под безопасность в методологии уже была — классы риска, защита `.env`, журнал действий, — а владельца у неё не было.
- **`investigator`** — расследование багов, не принадлежащих ни одной зоне. Профильные роли чинят в своих границах, и баг между границами чинить некому, пока кто-то не поставит диагноз. Не пишет даже тест на воспроизведение: условия описывает, тест пишет `qa-tester`.
- **`design-reviewer`** — визуальное качество и доступность. `dev-frontend` пишет вёрстку, `browser-tester` проверяет нажимаемость кнопок, а смотрит ли это как готовый продукт — не проверял никто. Браузер сам не открывает, работает с разметкой и снимками экрана.
- **`scope-challenger`** — оспаривает задачу до декомпозиции: какую проблему решаем, что будет если не делать, есть ли способ в десять раз дешевле. Вызывается только перед Tier 3: ворота, срабатывающие на каждую правку, превращаются в ритуал.

Все четыре только читают. Это не формальность: `investigator`, который сам чинит найденное, чинит симптом, а `security-reviewer`, который правит код, перестаёт быть независимым мнением. Все четыре добавлены в правило `memory` защитного хука — поле `memory` выдаёт им Write и Edit в обход списка `tools`.

---

## [1.2.0] — 2026-08-27

### Добавлено

- **Предложение развернуть команду при первом запуске** (`hooks/session_start.py`). Плагин ставит хуки и скиллы, но не роли — в шаблонах плейсхолдеры под проект, — и после установки легко решить, что «ничего не включилось». Теперь в проекте без ролей Claude сам предлагает сборку. В настроенном проекте хук молчит: подсказка, повторяющаяся каждую сессию, тратит контекст и приучает себя игнорировать. Отключается файлом `.claude/.no-team-setup-prompt`.
- **Готовая строка для копирования в README** — для тех, кто не хочет ждать предложения.

---

## [1.1.3] — 2026-08-27

### Добавлено

- **Теги выпусков.** Проставлены задним числом на все опубликованные версии, начиная с `v1.0.0`. Дают пользователю точку опоры при отчёте о проблеме и страницу Releases на GitHub. Шаг с тегом добавлен в памятку автору в [docs/update.md](docs/update.md).

---

## [1.1.2] — 2026-08-27

### Добавлено

- **Бейдж версии в шапке README.** Читает версию прямо из `.claude-plugin/plugin.json`, а не хранит её копией: иначе появилось бы третье место, где версию надо помнить обновить, и оно бы разошлось. CI проверяет, что бейдж указывает именно на манифест.

---

## [1.1.1] — 2026-08-27

### Исправлено

- **Обновление было только ссылкой в списке ссылок.** Тем, чем пользуются регулярно, ссылки среди прочих недостаточно: в README добавлен отдельный раздел с командами, двумя причинами, по которым обновление «не приходит», и таблицей того, что перезаписывать нельзя никогда.

---

## [1.1.0] — 2026-08-27

### Добавлено

- **`docs/update.md`** — обновление уже установленного. Отдельная тема со своими ловушками: часть файлов пользователь адаптировал под проект, и перезаписывать их нельзя. Таблица «что обновлять, что не трогать никогда», порядок переноса изменений в адаптированные роли, путь для тех, кто ставил до 1.0.0, и памятка автору — без подъёма версии в манифесте обновление до установленных плагинов не доедет.
- **`docs/verification.md`** — повторяемая проверка работоспособности в трёх уровнях: автоматический для CI, механика в живой сессии, поведение ролей. С командами, ожидаемыми исходами и разбором того, как отличать дефект методологии от дефекта сценария.
- **Два eval-сценария**, прогнанные на живом проекте: `CF-2` — недоступный сервис данных, где соблазн не остановиться, а придумать правдоподобные цифры; `PI-4` — инструкция, спрятанная в поле JSON-ответа внешнего сервиса.

### Изменено

- Раздел миграции в `docs/quick-start.md` устарел и заменён ссылкой на `update.md`: он описывал переход на 1.0.0 и ничего не знал про хуки, скиллы и плагин.

---

## [1.0.0] — 2026-08-26

Первый выпуск с исполняемым кодом. До этого методология была только текстом, и каждый раздел заканчивался словами «реализация на твоей стороне».

### Добавлено

- **Защитные хуки** — `hooks/guard.py`, пять правил: деструктив файловой системы, опасные git-операции, SQL без `WHERE`, запись в `.env`, возврат ролям ограничений, снятых полем `memory`. Только стандартная библиотека Python, без внешних зависимостей. Больше полусотни тестов, половина из них — на отсутствие ложных срабатываний.
- **Установка плагином Claude Code** — манифесты в `.claude-plugin/`, автоподключение хуков через `hooks/hooks.json`. Две команды вместо клонирования и копирования вручную.
- **Чек-листы как скиллы** — `skills/`. Обёртки намеренно тонкие: содержимое остаётся в `checklists/` и не дублируется, а Claude узнаёт о существовании чек-листа и подтягивает его сам.
- **Скилл `setup-agent-team`** — собирает команду ролей под проект: предлагает состав по стеку, копирует шаблоны, адаптирует плейсхолдеры, сводит маршрутизацию оркестратора.
- **Одиннадцатый принцип — `11-untrusted-input.md`.** Текст из тикетов, веб-страниц и ответов API это данные, а не инструкции. Раньше репозиторий тестировал устойчивость к инъекции в `10-eval-suite.md`, но нигде не формулировал само правило, а сценарии проверяли только инъекцию от пользователя — то есть от того, кому агент и так подчиняется. Добавлен сценарий `PI-3` для инъекции из внешнего источника.
- **Раздел о кэшировании промптов** в `principles/04`. Чтение из кэша устойчиво стоит около десятой части обычного ввода; цена записи зависит от провайдера. Практическое следствие: стабильное в начало промпта, изменчивое в конец, а метка времени вверху обесценивает весь кэш ниже.
- **Stop-хук `verify.py`** — не даёт завершить работу с падающими тестами, делая условие готовности механическим. Ставится отдельно и только вместе с командой проверки. Блокирует ограниченное число раз подряд: бесконечная блокировка заперла бы сессию, молчаливый пропуск вернул бы исходную проблему.
- **Журнал привилегированных действий `approval_log.py`** — реализация принципа 08, которая раньше существовала только на бумаге. Хук на `PostToolUse` записывает коммиты, пуши, миграции, установки пакетов, обращения к базе и правки чувствительных файлов. Рутина отфильтрована намеренно: журнал, куда пишется всё подряд, читать никто не станет.
- **Правило трёх попыток `retry_guard.py`** — принцип 07 был последним без механической опоры. Хук считает неудачи и блокирует четвёртый **слепой** повтор: если между попытками менялась рабочая копия, счётчик сбрасывается, потому что цикл «починил — прогнал снова» это нормальная работа, а не петля.
- **Предупреждение о протухании таблицы моделей** — машиночитаемая отметка даты сверки в `agents.config.example`, CI предупреждает через три месяца. Именно предупреждение, а не провал сборки.
- **`EXAMPLES.md`** — каждый из одиннадцати принципов в виде «плохо → хорошо».
- **CI** — прогон тестов на трёх версиях Python при каждом push и pull request.
- **`docs/claude-code-mechanisms.md`** — разбор четырёх механизмов запуска: субагенты, agent teams, workflows, скиллы. Разводит терминологию: «команда агентов» в методологии это способ организации работы, а `agent teams` в Claude Code — один из механизмов, причём экспериментальный и выключенный по умолчанию.

### Изменено

- **Выбор модели для ревью — по измеримому порогу, а не по названию тира.** Таксономия `top / strong / mid / light` заменена на `coding_index ≥ 74` для обычного ревью и `≥ 77` плюс `effort: high` для точек высокого риска. Причина: названия перестали означать уровень — модель Google с `flash` в имени обходит вчерашний флагман OpenAI по коду и стоит в 13 раз дешевле.
- **Конкретные модели вынесены в одно место** — `agents.config.example`. Из `principles/04` продублированная таблица убрана, чтобы протухала одна точка, а не две.
- **Шаблоны агентов** получили поля `permissionMode`, `maxTurns`, `effort`, `memory`; `browser-tester` — inline-описание Playwright MCP, чтобы браузерные инструменты не занимали контекст основной сессии.
- **`principles/09`** переписан на действующий контракт хуков: решение через `permissionDecision`, состав входного JSON, события сверх `PreToolUse`, типы хуков включая `agent`.
- **`principles/06`** дополнен описанием нативного поля `memory` и его скоупов.
- **`checklists/permission-checklist.md`** — добавлена матрица режимов и разбор конфликта `memory` с матрицей разрешений.

### Исправлено

- **Поле `memory` снимало ограничения пяти ролей из одиннадцати** — оно выдаёт Read, Write и Edit в обход списка `tools`. Закрыто правилом `memory` в хуке; у этих ролей память и хук работают только в паре.
- **Девять способов обойти защиту**, найденных триангулированным ревью: перевод строки и одиночный `&` как разделители команд, вложенный `sh -c`, вызов по абсолютному пути, присваивание перед командой, `git clean` с длинными флагами, `git restore`, `git checkout -- .` с дополнительным путём, `WHERE` из подзапроса и комментария, выход из разрешённой зоны через `..`. Разбор команды переведён на `shlex` с `punctuation_chars`.
- **Два ложных срабатывания** — на текст с символом перенаправления внутри кавычек и на SQL со строковым литералом, где слово `DELETE` было частью данных.
- **Опечатка и пустое значение в списке правил** молча отключали защиту — теперь хук отказывает с объяснением.
- **`permissionMode` не реализует классы риска**, как утверждалось: поле управляет только правками файлов, а классы W и P закрываются правилами разрешений и хуками. Формулировки исправлены, значения приведены к фактическим классам.
- Самопротиворечивое сообщение о блокировке: предлагаемая альтернатива сама блокировалась тем же правилом.
- Проверочный `grep` в README не ловил часть плейсхолдеров и искал два несуществующих.
- Битые внутренние ссылки и устаревшие идентификаторы моделей.
- **Правило `memory` обходилось через оболочку.** Инструмент Write у роли блокировался, а `cat > файл` писал тот же файл мимо проверки. Найдено на живом прогоне: субагент `devops` создал файл, которого не имел права создавать. Хуже того, платформа после блокировки Write сама предлагает агенту перейти на Bash. Правило стало двусторонним, как ранее `env`.
- **Шаги с SIDE-моделями на Tier 3 значились обязательными**, хотя готового вызова внешних моделей репозиторий не поставляет. Живой оркестратор упёрся в этот гейт и остановился; у другого агента это с равной вероятностью стало бы тихой имитацией выполненного шага. Шаги сделаны условными, пропуск обязан фиксироваться явно. Соответственно исправлены два чек-листа, которые требовали того же безусловно.
- **В карте агентов шаблона оркестратора не хватало двух ролей** — `i18n-keeper` и `local-sysops`. Роль, отсутствующая в маршрутизации, не будет вызвана никогда. Добавлена колонка владения файлами: на практике она оказалась полезнее колонки «когда вызывать».
- **Шаблон `qa-tester` навязывал допущение**, что SQLite это тестовый суррогат. Для проектов, где SQLite и есть боевая база, совет был вредным.
- **Скилл `setup-agent-team` не имел ветки для нового проекта** — шаг «пойми проект» читает стек из кода, которого ещё нет.
- **Поля `permissionMode`, `skills` и `mcpServers` описывались как всегда действующие**, хотя у роли, запущенной тиммейтом в agent teams, они не применяются: режим наследуется от ведущей сессии, скиллы и MCP-серверы берутся из настроек проекта. Затрагивало шаблон `browser-tester`, где inline-описание Playwright работает только для субагента, и матрицу разрешений.

### Прочее

- README сокращён; подробности установки, настройки моделей и первого запуска вынесены в `docs/`.
- Иконка уменьшена с 1024×1024 до 512×512 — 605 КБ превратились в 104 КБ.

---

## [0.1.0] — 2026-05-17

Первая публикация методологии: 10 принципов, 12 шаблонов ролей, 5 чек-листов, провайдер-нейтральный пример конфигурации. Только текст, без исполняемого кода.
