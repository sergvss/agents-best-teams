# OS / FS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.idea/
.vscode/
.vs/
*.swp
*.swo
*~

# Local notes / scratch
scratch/
notes/
tmp/
*.local.md

# Maintainer brief — приватная инструкция для агента-сопровождающего
MAINTAINER_BRIEF.md

# Если кто-то добавит tooling
node_modules/
__pycache__/
*.pyc
venv/
.venv/

# Логи
*.log
logs/

# Секреты (на всякий случай — в публичной репе их быть не должно)
.env
.env.*
!.env.example
secrets/
*.key
*.pem

# ─────────────────────────────────────────────────────────────────────────
# Maintainer-инфраструктура — приватная папка агента-сопровождающего:
# env с ключами, ledger расходов, grep-паттерны, личные заметки.
# Целиком в gitignore — в публичный репо ничего отсюда не попадает.
# Публичный пример конфига для ПОЛЬЗОВАТЕЛЕЙ репо — в корне как
# `agents.config.example`, не здесь.
# ─────────────────────────────────────────────────────────────────────────
.maintainer/
